# Simulation package imports ---------------------------------------------------
#
# The SCM data generator currently uses base R only. Keep package imports
# centralized here so future simulation dependencies have one obvious home.

invisible(TRUE)
