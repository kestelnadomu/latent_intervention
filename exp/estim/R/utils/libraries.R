# Estimation package imports ---------------------------------------------------
#
# The first estimation implementation uses base R only. Keep package imports
# centralized here so a future alternative backend (for example Stan) has one
# obvious place for dependency checks and setup.

invisible(TRUE)
